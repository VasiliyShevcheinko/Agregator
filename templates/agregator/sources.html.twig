{% extends 'base.html.twig' %}

{% block title %}Sources{% endblock %}

{% block body %}
    <div class="wrapper">
        <div class="wrapper">
            <h1>Источники</h1>
            <ul>
                {% for sourceName,sourceHref in sources %}
                    <li><a href="{{ sourceHref }}">{{ sourceName }}</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}
